name: prueba_partners
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  freezed_annotation: ^3.0.0
  dio: ^5.8.0+1
  flutter_riverpod: ^2.6.1
  dartz: ^0.10.1
  json_serializable: ^6.9.4
  go_router: ^14.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  freezed: ^3.0.3
  build_runner: ^2.4.15

flutter:

  uses-material-design: true

  assets:
    - assets/images/
